export default function SavedThemes() {
  return (
    <>
      <section className="mb-12 flex-1">
        view sameple ui with theme vars applied
      </section>
      <aside className="flex flex-1 flex-col border-b border-neutral-200 bg-white md:max-w-[512px] md:border-l">
        - Select a theme - View theme details - Option to update theme (if
        changes are made) - Option to delete theme
      </aside>
    </>
  );
}
